# accounting
Мини бухгалтерия (в разработке)<br>
Для ознакомления<br>

1)clone the repository<br>
2)docker compose -f ./docker/docker-compose.yml build 
3)docker compose -f ./docker/docker-compose.yml up -d